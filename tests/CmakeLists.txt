add_library(test_runner STATIC test-runner.cpp)
target_include_directories(test_runner PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})

add_executable(test_tecslog_init test-init.cpp test-main.cpp)
if (BUILD_SHARED_LIBS)
  target_link_libraries(test_tecslog_init PRIVATE ${PROG_LIBRARY}_shared test_runner)
else()
  target_link_libraries(test_tecslog_init PRIVATE ${PROG_LIBRARY} test_runner)
endif()

add_executable(test_tecslog_level test-level.cpp test-main.cpp)
if (BUILD_SHARED_LIBS)
  target_link_libraries(test_tecslog_level PRIVATE ${PROG_LIBRARY}_shared test_runner)
else()
  target_link_libraries(test_tecslog_level PRIVATE ${PROG_LIBRARY} test_runner)
endif()

add_executable(test_tecslog_log test-log.cpp test-main.cpp)
if (BUILD_SHARED_LIBS)
  target_link_libraries(test_tecslog_log PRIVATE ${PROG_LIBRARY}_shared test_runner)
else()
  target_link_libraries(test_tecslog_log PRIVATE ${PROG_LIBRARY} test_runner)
endif()

add_test(NAME test-tecslog-init COMMAND test_tecslog_init)
add_test(NAME test-tecslog-level COMMAND test_tecslog_level)
add_test(NAME test-tecslog-log COMMAND test_tecslog_log)
